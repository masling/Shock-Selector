# Copy only needed values into an ignored local/deployment environment.
# Does not enable emails by itself; never commit actual credentials.
TRANSACTIONAL_EMAIL_ENABLED=false
SMTP_HOST=smtp.zeptomail.com
SMTP_PORT=587
SMTP_USERNAME=emailapikey
SMTP_PASSWORD=
# Sender is the confirmed service@vibroabsorber.com. Domain must be verified in ZeptoMail.
# Supabase Auth SMTP must be configured separately in its dashboard.
